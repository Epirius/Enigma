class Rotor (which rotor?, position in the machine, position wrt the letter on top, direction letter is going)
	-rotate the rotor by one (based on the position in the machine aswell)
	-rotate the next rotor when hitting notch
	-get key(based on incoming letter)
	-reverse(if the direction is coming back out)

plugboard

reflector